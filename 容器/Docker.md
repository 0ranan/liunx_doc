# Docker 容器

## 术语速查

Image(镜像)
> docker build 构建出来的那个东西就是镜像（image）,镜像只需要构建一次后面是一个只读的存在

Container(容器)
> 镜像运行起来就是容器（container）,容器是一个运行中的进程,可以理解为是一个轻量级的虚拟机

Dockerfile(镜像构建文件)
> 用来生成Image的文件,里面包含了镜像的构建指令

docker-compose(容器编排文件)
> 用来定义和运行多个容器的文件,可以定义容器之间的依赖关系,网络配置,卷挂载等